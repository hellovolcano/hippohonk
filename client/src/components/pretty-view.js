import './pretty-view.css'

const PrettyView = () => {
    // on load, have hippo slide in from the left

    // on scroll, have hippo slide to the right out of view
    // have the header bar slide down to display
    // have the 
    return(
        <div className="hero">
            <div className="big-hippo-title">
                <h1>Music Festival Ratings</h1>
                <h2>By music fanatics like you.</h2>
            </div>
        </div>
    )
}

export default PrettyView